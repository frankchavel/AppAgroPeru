<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="2dp"
    app:cardBackgroundColor="#FFFFFF">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Ícono inicial del producto -->
        <TextView
            android:id="@+id/tv_icono_producto"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@drawable/bg_circle_green_light"
            android:gravity="center"
            android:text="🌽"
            android:textSize="22sp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>

        <!-- Nombre del producto -->
        <TextView
            android:id="@+id/tv_nombre_producto"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:text="Papa Yungay"
            android:textColor="#0C1324"
            android:textSize="15sp"
            android:textStyle="bold"
            app:layout_constraintStart_toEndOf="@id/tv_icono_producto"
            app:layout_constraintEnd_toStartOf="@id/tv_precio_promedio"
            app:layout_constraintTop_toTopOf="@id/tv_icono_producto" />

        <!-- Procedencia -->
        <TextView
            android:id="@+id/tv_procedencia"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginTop="4dp"
            android:drawableStart="@android:drawable/ic_menu_mylocation"
            android:drawablePadding="4dp"
            android:drawableTint="#78909C"
            android:text="Huánuco"
            android:textColor="#78909C"
            android:textSize="12sp"
            app:layout_constraintStart_toEndOf="@id/tv_icono_producto"
            app:layout_constraintEnd_toStartOf="@id/tv_precio_promedio"
            app:layout_constraintTop_toBottomOf="@id/tv_nombre_producto" />

        <!-- Precio promedio (derecha) -->
        <TextView
            android:id="@+id/tv_precio_promedio"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="S/ 0.85"
            android:textColor="#00C853"
            android:textSize="18sp"
            android:textStyle="bold"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@id/tv_icono_producto" />

        <!-- Etiqueta "por kg" -->
        <TextView
            android:id="@+id/tv_unidad"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="por kg"
            android:textColor="#78909C"
            android:textSize="11sp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tv_precio_promedio" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>